# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #

animations {
    enabled = true

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 1, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 1, easeOutQuint #4.79
    # animation = windowsIn, 1, 1, easeOutQuint, popin 87% #4.1
    # animation = windowsOut, 1, 1, easeOutQuint, popin 87% #1.49

    # animation = fadeIn, 1, 1.73, almostLinear
    # animation = fadeOut, 1, 1.46, almostLinear
    # animation = fade, 1, 3.03, quick

    animation = layers, 1, 1, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear

    animation = workspaces, 1, 1, easeOutQuint
    animation = workspacesIn, 1, 1, easeOutQuint
    animation = workspacesOut, 1, 1, easeOutQuint
  # enabled = yes
  #
  # bezier = wind, 0.05, 0.9, 0.1, 1.05
  # bezier = winIn, 0.1, 1.1, 0.1, 1.1
  # bezier = winOut, 0.3, -0.3, 0, 1
  # bezier = liner, 1, 1, 1, 1
  # bezier = overshot, 0.05, 0.9, 0.1, 1.05
  # bezier = smoothOut, 0.5, 0, 0.99, 0.99
  # bezier = smoothIn, 0.5, -0.5, 0.68, 1.5
  #
  # animation = windows, 1, 1, wind, slide
  # animation = windowsIn, 1, 1, winIn, slide
  # animation = windowsOut, 1, 1, smoothOut, slide
  # animation = windowsMove, 1, 1, wind, slide
  # animation = border, 1, 1, liner
  # animation = borderangle, 1, 180, liner, loop #used by rainbow borders and rotating colors
  # animation = fade, 1, 3, smoothOut
  # animation = workspaces, 1, 5, liner
  #
  # # animations for -git or version >0.42.0
  # animation = workspacesIn, 1, 3, winIn, slide
  # animation = workspacesOut, 1, 3, winOut, slide
}
